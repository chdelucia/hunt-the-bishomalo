{
  "name": "instructions-shell",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/instructions/shell/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:instructions", "type:shell"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "libs/instructions/shell/jest.config.ts",
        "tsConfig": "libs/instructions/shell/tsconfig.spec.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}

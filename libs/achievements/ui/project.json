{
  "name": "achievements-ui",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/achievements/ui/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:achievements", "type:ui"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "storybook": {
      "executor": "@storybook/angular:start-storybook",
      "options": {
        "port": 4400,
        "configDir": "libs/achievements/ui/.storybook",
        "browserTarget": "hunt-the-bishomalo:build",
        "compodoc": false
      },
      "configurations": {
        "ci": {
          "quiet": true
        }
      }
    },
    "build-storybook": {
      "executor": "@storybook/angular:build-storybook",
      "outputs": ["{options.outputDir}"],
      "options": {
        "outputDir": "dist/storybook/achievements-ui",
        "configDir": "libs/achievements/ui/.storybook",
        "browserTarget": "hunt-the-bishomalo:build",
        "compodoc": false
      },
      "configurations": {
        "ci": {
          "quiet": true
        }
      }
    },
    "test-storybook": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test-storybook -c libs/achievements/ui/.storybook --url=http://localhost:4400"
      }
    },
    "static-storybook": {
      "executor": "@nx/web:file-server",
      "dependsOn": ["build-storybook"],
      "options": {
        "buildTarget": "hunt-the-bishomalo:build",
        "staticFilePath": "dist/storybook/achievements-ui",
        "spa": true
      },
      "configurations": {
        "ci": {
          "buildTarget": "hunt-the-bishomalo:build:ci"
        }
      }
    }
  }
}
